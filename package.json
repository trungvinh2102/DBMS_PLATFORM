{
  "name": "quriodb",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20",
    "bun": ">=1.3.6"
  },
  "scripts": {
    "dev": "turbo -F web -F api dev",
    "dev:web": "turbo -F web dev",
    "dev:backend": "turbo -F api dev",
    "start": "turbo start",
    "build": "turbo build",
    "desktop:build": "bun run version:sync && powershell -ExecutionPolicy Bypass -Command \"& ./build-backend.ps1 -Target gnu; \\$env:RUSTUP_TOOLCHAIN='stable-x86_64-pc-windows-gnu'; \\$env:PATH='C:/ProgramData/mingw64/mingw64/bin;' + \\$env:PATH; cd apps/desktop; bun run tauri build --target x86_64-pc-windows-gnu\"",
    "desktop:sidecar:linux": "bun run version:sync && bash apps/desktop/scripts/build-sidecar.sh",
    "desktop:build:linux": "bun run version:sync && bash apps/desktop/scripts/build-sidecar.sh && cd apps/desktop && bun run tauri build --target x86_64-unknown-linux-gnu",
    "desktop:dev:linux": "bun run version:sync && bash apps/desktop/scripts/build-sidecar.sh && cd apps/desktop && bun run tauri dev --target x86_64-unknown-linux-gnu",
    "desktop:dev:windows": "bun run version:sync && cd apps/desktop && bun run tauri dev --target x86_64-pc-windows-gnu",
    "version:sync": "bun scripts/sync-version.ts",
    "version:check": "bun scripts/sync-version.ts --check",
    "db:start": "docker compose up -d",
    "db:watch": "docker compose up",
    "db:stop": "docker compose stop",
    "db:down": "docker compose down",
    "check-types": "turbo check-types",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "clean": "rm -rf node_modules .turbo",
    "test:rag": "cd apps/api && python -m pytest -m rag"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "prettier": "^3.8.1",
    "turbo": "^2.6.3",
    "typescript": "^5.6.3"
  }
}
